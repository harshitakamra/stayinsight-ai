services:
  - type: web
    name: stayinsight-backend
    env: python
    rootDir: backend
    buildCommand: python -m pip install --upgrade pip setuptools wheel && python -m pip install --only-binary=:all: --prefer-binary -r requirements.txt
    startCommand: python -m uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: RENDER_PYTHON_VERSION
        value: 3.12.0
      - key: JWT_SECRET
        value: stayinsight-super-secret-key-2026
      - key: CORS_ALLOWED_ORIGINS
        value: http://localhost:5173
